<% if !Like.exists?(user_id: current_user.id, article_id: article.id)%>
<%= button_to "お気に入り登録", article_likes_path(article.id), remote: true, method: :post %>
<% else %>
<%= button_to "お気に入り解除", article_likes_path(article.id), remote: true, method: :delete %>
<% end %>